# Copy this file to .env and fill in real values. .env is gitignored.

# Only used by controller/data_manipulation.py's dictionary/word-scan feature
# (calls dictionaryapi.dev directly) - curriculum generation no longer uses this.
myGptKey=

# empowerHSA API (D:\empowerHSA) - all curriculum generation and storage lives
# there now. Must match the API_KEY set in empowerHSA's own .env.
EMPOWER_HSA_BASE_URL=http://127.0.0.1:8000
EMPOWER_HSA_API_KEY=
